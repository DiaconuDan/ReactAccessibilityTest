export interface Interval {
  start: Date;
  end: Date;
}

export interface IFormInputs {
    // dropdownValues: string[],
    textInput: string;
    numericInput: number;
    dropdown: string;
    startDate: Date;
    endDate: Date;
  }